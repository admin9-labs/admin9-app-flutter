# Changelog

## 1.0.2 - 2026-07-31

- Clarified risk-tiered Phase 6 evidence ownership: deterministic contracts are
  automated, human review samples one representative flow per platform for
  reader/system/IME/safe-area/release facts, P0/P1 remain blockers, and named
  P2/P3 device repetition stays backlog rather than being mislabeled passed.
- Added fixed P0-P3 definitions, backlog ownership/triggers and the explicit
  boundary that representative-flow evidence is a minimum usable baseline, not
  WCAG or complete assistive-technology certification.
- Closed Phase 6 after hash-bound Android/iOS release delivery, representative
  TalkBack/VoiceOver and real-IME flows, Android API 34/36 predictive back,
  API 36 edge-to-edge, iOS edge-back and App Extra Large safe-area evidence.

## 1.0.1 - 2026-07-29

- Clarified that Phase 0D implements honest non-visual contracts and boundary mechanisms, while each concrete visual Widget is instantiated and exported only in its assigned implementation phase.
- Added the repository-local exact public contract barrel, manifest-derived Brand/App-identity generator and verifier, token lookup scope, precise Gallery registry guard, analyzer-AST Business/Core/App import fixtures, and implementation probe without changing frozen tokens, API shapes, platform mappings, or product decisions.

## 1.0.0 - 2026-07-29

- Established Admin9 Design System as the single specification source.
- Separated Admin9 Core, Brand Theme, and Business Layer.
- Froze one measurable light/dark visual direction and dual-platform mappings.
- Froze component, page-pattern, derived-project, accessibility, and governance contracts.
- Added `AppActionMenu`, `AppProgressIndicator`, the exact `1.24` Extra Large calibration, and one canonical automated matrix.
- Added the manifest schema, approved compatibility registry, canonical Brand/asset SHA-256 evidence, one valid and eleven invalid fixtures, dependency-free validator, stable rule/document links, and non-exported Dart consumer-shape declaration probe.
- Froze controller-only Business entry for Dialog/ActionMenu/Feedback presentation and the public export matrix.
- Synchronized the downstream implementation plan and recorded remaining implementation/device Unknowns.

The v1.0.0 specification commit itself contained no Flutter runtime
implementation, page migration, theme change, or stable exported Dart library.
Runtime implementation was delivered later through Phase 0D-6 commits and is
versioned independently from this normative document history.
